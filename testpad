#
Planned item
Status
Work completed / remaining
Estimated time
1
Understand F2FS GC victim-segment selection
✅ Completed
Understood how victim segment is selected and how GC enters data-segment cleaning
—
2
Trace valid/invalid blocks in victim segment
✅ Completed
Added validity tracing in gc_data_segment() to identify valid and invalid blocks
—
3
Understand valid-block selection criteria
✅ Completed
Established the flow: check_valid_map() → is_alive() → inode/dnode validation → block selected for migration
—
4
Trace end-to-end block migration
✅ Completed
Added FLOW identification to correlate source block → destination block → migration completion
—
5
Identify source PBA of valid blocks
✅ Completed
Source physical block address is identified from victim segment/offset and existing GC migration path
—
6
Identify destination-segment/block allocation
✅ Completed
Understood that F2FS allocates a new destination block through f2fs_allocate_data_block()
—
7
Understand existing CPU-based migration
✅ Completed
Analyzed move_data_block() / move_data_page(): source read → memory copy → destination write → mapping update
—
8
Identify copy-offload insertion point
🟡 In progress
Targeting DATA GC path after valid-block identification and destination allocation, while bypassing normal READ/memcpy()/WRITE on successful offload
0.5 day
9
Analyze attached block-copy-offload patch
🟡 In progress
Need to map blk_copy_in_seg, blk_copy_out_seg, blkdev_copy_offload() and associated limits/completion semantics to F2FS
0.5–1 day
10
Design source parameters
⏳ Yet to start
Prepare source PBA, block count/data length, device and range information for blk_copy_in_seg
0.5 day
11
Design destination parameters
⏳ Yet to start
Prepare destination PBA, length and device information for blk_copy_out_seg
0.5 day
12
Implement copy-offload path in F2FS GC
⏳ Yet to start
Add offload path to gc_data_segment() / migration flow
1–1.5 days
13
Preserve F2FS metadata updates
⏳ Yet to start
Ensure destination allocation, node mapping, SIT/SSA state and source invalidation remain correct
1 day
14
Implement offload failure fallback
⏳ Yet to start
If blkdev_copy_offload() fails, correctly return to existing move_data_block() / move_data_page() path without corrupting allocation state
1 day
15
Add copy-offload debug tracing
⏳ Yet to start
Add COPY_OFFLOAD_ATTEMPT, SUCCESS, FAILED, FALLBACK, and final migration method using existing FLOW ID
0.5 day
16
Support contiguous-block/range offload
⏳ Yet to start
Initially support one block; subsequently group contiguous valid blocks into larger copy ranges for efficiency
1–2 days
17
Build/compile validation
⏳ Yet to start
Compile kernel/F2FS with the modified block-copy interfaces and resolve API/configuration issues
0.5–1 day
18
Functional GC testing
⏳ Yet to start
Verify victim selection, valid-block selection, source/destination addresses and successful offload
1 day
19
Failure/fallback testing
⏳ Yet to start
Force/induce copy-offload failure and verify normal GC migration is used
0.5–1 day
20
Performance comparison
⏳ Yet to start
Compare normal GC READ+WRITE against device copy offload: latency, I/O count and CPU overhead
1–2 days
21
Final documentation
⏳ Yet to start
Document architecture, code changes, traces, test results and performance results
0.5–1 day
