Studied F2FS GC flow through gc_data_segment(), victim selection, valid-block migration, move_data_block(), move_data_page(), f2fs_do_write_data_page(), f2fs_outplace_write_data(), do_write_page(), and f2fs_allocate_data_block().
Added F2FS debug instrumentation to correlate victim segment/offset, source PBA, destination PBA, inode/index and migration flow.
Integrated the block-copy offload series needed to expose queue limits, REQ_OP_COPY_SRC/DST and blkdev_copy_offload().
Initially validated the concept on QEMU and corrected the ordering so source-read/GC state handling was not broken before destination allocation.
Created a reusable f2fs_gc_try_copy_offload() helper that converts F2FS PBAs to device sectors and submits the copy request.
Integrated offload into move_data_block() and into the deferred write path for move_data_page() via do_write_page().
Moved testing to the real SM8850 platform with a Kioxia 512 GB UFS 4.1 device.
Resolved the previous crash and captured a complete GC/offload trace.
